---
phase: 11-graph-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_ui_command.py
  - tests/test_ui_server.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "All 8 test stubs exist and fail (RED) on unmodified codebase"
    - "Test file structure matches the module paths that Plans 11-02 and 11-04 will create"
  artifacts:
    - path: "tests/test_ui_command.py"
      provides: "CLI behavior tests (UI-01, UI-02, UI-03)"
      exports: ["TestUICommand"]
    - path: "tests/test_ui_server.py"
      provides: "FastAPI server tests (UI-01, UI-02)"
      exports: ["TestAPIRoutes", "TestAppFactory"]
  key_links:
    - from: "tests/test_ui_command.py"
      to: "src.cli.commands.ui"
      via: "import at test runtime"
      pattern: "from src.cli.commands.ui import ui_command"
    - from: "tests/test_ui_server.py"
      to: "src.ui_server.app"
      via: "import at test runtime"
      pattern: "from src.ui_server.app import create_app"
---

<objective>
Write the Wave 0 RED test scaffold for Phase 11 — all 8 tests must exist and fail (ImportError / AttributeError) because the modules they test do not exist yet. Plans 11-02 and 11-04 will turn these GREEN.

Purpose: Establish test contracts before implementation so executors cannot ship code that skips a behavior.
Output: `tests/test_ui_command.py` + `tests/test_ui_server.py` — 8 failing tests covering UI-01, UI-02, UI-03.
</objective>

<execution_context>
@/home/tasostilsi/.claude/get-shit-done/workflows/execute-plan.md
@/home/tasostilsi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-graph-ui/11-CONTEXT.md
@.planning/phases/11-graph-ui/11-RESEARCH.md
@.planning/phases/11-graph-ui/11-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI test scaffold (RED) — test_ui_command.py</name>
  <files>tests/test_ui_command.py</files>
  <behavior>
    - test_help: `graphiti ui --help` exits 0 and help text contains "--global"
    - test_missing_static_dir: when `ui/out/` does not exist, command exits non-zero with message containing "static" or "ui/out"
    - test_port_conflict: when the port is already bound, command exits non-zero with message containing "already in use" or the port number
    - test_global_flag: `--global` flag is accepted (no error on parse), scope label contains "global"
  </behavior>
  <action>
Create `tests/test_ui_command.py` with 4 test stubs. All tests must import from `src.cli.commands.ui` which does not exist yet — the file must be written so tests fail with ImportError or AttributeError on the unmodified codebase.

Use `typer.testing.CliRunner` pattern from existing tests (see `tests/test_cli_commands.py` for the established pattern).

Structure the tests to exercise the CLI surface as it will be built in Plan 11-04:
- `from src.cli.commands.ui import ui_command` at top of file — causes RED on import
- Use `CliRunner` to invoke `ui_command` via a minimal test app: `test_app = typer.Typer(); test_app.command()(ui_command)`

Tests:
1. `TestUICommand::test_help` — `runner.invoke(test_app, ["--help"])` → result.exit_code == 0 and "--global" in result.output
2. `TestUICommand::test_missing_static_dir` — patch `ui/out/` to not exist, invoke with no flags → exit_code != 0 and any of ["static", "ui/out", "missing", "not found"] in result.output.lower()
3. `TestUICommand::test_port_conflict` — patch `socket.socket.bind` to raise `OSError`, invoke → exit_code != 0 and any of ["in use", "conflict", str(8765)] in result.output
4. `TestUICommand::test_global_flag` — patch out uvicorn.run to avoid hanging, invoke with `["--global"]` → command accepts flag without argparse error
  </action>
  <verify>
    <automated>cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && python -m pytest tests/test_ui_command.py -x -q 2>&1 | head -30</automated>
  </verify>
  <done>4 tests collected, all fail with ImportError or similar — NOT "no tests ran" or "4 passed"</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server test scaffold (RED) — test_ui_server.py</name>
  <files>tests/test_ui_server.py</files>
  <behavior>
    - test_graph_endpoint: GET /api/graph returns JSON with "nodes" and "links" keys, both lists
    - test_node_detail_endpoint: GET /api/nodes/{uuid} returns JSON with "id", "name", "entityType" keys
    - test_static_mount: create_app() with a tmp dir for static export — the app mounts StaticFiles at "/"
    - test_ui_ports_from_toml: load_config() with [ui] api_port = 9999 → config.ui_api_port == 9999
  </behavior>
  <action>
Create `tests/test_ui_server.py` with 4 test stubs. All tests must fail RED on the unmodified codebase because `src.ui_server` does not exist yet.

Tests:
1. `TestAPIRoutes::test_graph_endpoint` — import `create_app` from `src.ui_server.app`; use `fastapi.testclient.TestClient`; mock `GraphService.list_entities` to return `[]` and `GraphService.list_edges` to return `[]`; GET `/api/graph` → status_code == 200, response.json() has "nodes" and "links" keys
2. `TestAPIRoutes::test_node_detail_endpoint` — mock `GraphService.get_entity` to return a dict with uuid/name/labels/summary; GET `/api/nodes/test-uuid-123` → status_code == 200, response.json() has "id", "name", "entityType"
3. `TestAppFactory::test_static_mount` — create a tmp dir with a minimal `index.html`; call `create_app(scope_label="project", static_dir=tmp_path)`; assert the app has a route at "/" that serves static files (check `app.routes` for a Mount with path "/")
4. `TestLLMConfigUI::test_ui_ports_from_toml` — write TOML `[ui]\napi_port = 9999\nui_port = 4000\n` to tmp file; `load_config(config_path=...)` → config.ui_api_port == 9999 and config.ui_port == 4000

Anti-pattern: Do NOT call `asyncio.run()` inside tests — use `AsyncClient` or mock at the service layer so route handlers never execute real async graph calls.
  </action>
  <verify>
    <automated>cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && python -m pytest tests/test_ui_server.py -x -q 2>&1 | head -30</automated>
  </verify>
  <done>4 tests collected, all fail with ImportError or AttributeError — NOT "4 passed"</done>
</task>

</tasks>

<verification>
After both tasks:
```
cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && python -m pytest tests/test_ui_command.py tests/test_ui_server.py -v 2>&1 | tail -20
```
Expected: 8 tests FAILED (not ERROR-skipped, not PASSED). All failures must be import-level or assertion-level indicating the production module is missing.

Full suite must still pass (these new tests fail in isolation, not the rest):
```
cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && python -m pytest tests/ -q --ignore=tests/test_ui_command.py --ignore=tests/test_ui_server.py 2>&1 | tail -5
```
</verification>

<success_criteria>
- `tests/test_ui_command.py` exists with 4 tests, all RED
- `tests/test_ui_server.py` exists with 4 tests, all RED
- Existing 272+ tests still pass (new test files do not pollute the passing suite via conftest side effects)
- Test names match exactly what VALIDATION.md specifies so Plan 11-04 can turn them GREEN without renaming
</success_criteria>

<output>
After completion, create `.planning/phases/11-graph-ui/11-01-SUMMARY.md`
</output>
