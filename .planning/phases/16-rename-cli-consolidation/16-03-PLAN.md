---
phase: 16-rename-cli-consolidation
plan: "03"
type: execute
wave: 2
depends_on:
  - "16-01"
  - "16-02"
files_modified:
  - src/cli/commands/add.py
  - src/cli/commands/capture.py
  - src/cli/commands/compact.py
  - src/cli/commands/hooks.py
  - src/cli/commands/memory.py
  - src/cli/commands/mcp.py
  - src/cli/commands/queue_cmd.py
  - src/cli/commands/show.py
  - src/cli/commands/stale.py
  - src/cli/commands/summarize.py
  - src/cli/commands/sync.py
  - src/hooks/session_start.py
  - src/hooks/capture_entry.py
  - src/hooks/session_stop.py
  - src/hooks/inject_context.py
  - src/hooks/manager.py
  - src/hooks/installer.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
must_haves:
  truths:
    - "11 deleted command files no longer exist on disk (index.py is kept)"
    - "All four Phase 15 hook scripts invoke `recall` not `graphiti`"
    - "`installer.py` `install_global_hooks()` writes `recall` binary path, not `graphiti`"
    - "No import errors in `src/cli/__init__.py` from missing command files"
    - "`session_start.py` calls GitIndexer directly — no subprocess to `recall sync`"
  artifacts:
    - path: src/hooks/session_start.py
      provides: "SessionStart hook calling GitIndexer directly (no subprocess)"
      contains: "GitIndexer"
    - path: src/hooks/capture_entry.py
      provides: "PostToolUse hook — no graphiti binary references"
      contains: "recall"
    - path: src/hooks/session_stop.py
      provides: "SessionStop hook — no graphiti binary references"
      contains: "recall"
    - path: src/hooks/inject_context.py
      provides: "InjectContext hook — no graphiti binary references"
      contains: "recall"
    - path: src/hooks/manager.py
      provides: "Hook manager calling recall binary"
      contains: "_RECALL_CLI"
    - path: src/hooks/installer.py
      provides: "global hooks installer using recall binary name"
      contains: "recall"
  key_links:
    - from: src/hooks/session_start.py
      to: src/indexer.GitIndexer
      via: "GitIndexer(project_root=project_root).run(full=False) called directly"
      pattern: "GitIndexer.*run"
    - from: src/hooks/manager.py
      to: recall binary
      via: "_RECALL_CLI = str(Path(sys.executable).parent / 'recall')"
      pattern: "_RECALL_CLI.*recall"
    - from: src/hooks/installer.py
      to: recall binary
      via: "install_global_hooks() builds command strings using recall"
      pattern: "recall"
---

<objective>
Delete the 11 removed command files from disk (index.py is KEPT as a hidden command) and update all four Phase 15 hook scripts to call `recall` instead of `graphiti`. Update `session_start.py` to call `GitIndexer` directly instead of via subprocess.

Purpose: With Plan 01 removing the 11 command registrations from `__init__.py`, those source files are now dead code. Deleting them prevents confusion. Updating hook binary references ensures all Phase 15 hooks continue working after the rename. `session_start.py` is simplified to call `GitIndexer` directly — there is no longer a `recall sync` subprocess to call (sync is fully deleted; incremental indexing happens via the indexer directly).

Output:
- 11 command files deleted (index.py is kept)
- All four hook scripts updated: `graphiti` → `recall` binary references
- `session_start.py` updated: replaces subprocess `recall sync` with direct `GitIndexer` call
- `manager.py` updated: `_GRAPHITI_CLI` → `_RECALL_CLI` pointing to `recall` binary
- `installer.py` updated: `install_global_hooks()` command strings use `recall` binary
</objective>

<execution_context>
@/home/tasostilsi/.claude/get-shit-done/workflows/execute-plan.md
@/home/tasostilsi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-rename-cli-consolidation/16-CONTEXT.md
@.planning/phases/16-rename-cli-consolidation/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 11 removed command files (index.py is kept)</name>
  <files>
    src/cli/commands/add.py,
    src/cli/commands/capture.py,
    src/cli/commands/compact.py,
    src/cli/commands/hooks.py,
    src/cli/commands/memory.py,
    src/cli/commands/mcp.py,
    src/cli/commands/queue_cmd.py,
    src/cli/commands/show.py,
    src/cli/commands/stale.py,
    src/cli/commands/summarize.py,
    src/cli/commands/sync.py
  </files>
  <read_first>
    - src/cli/__init__.py (confirm none of the 11 files are still imported — after Plan 01 these should all be absent)
  </read_first>
  <action>
Delete these 11 files from the filesystem. Use `Path.unlink()` or shell `rm` for each.

Files to delete (all under `src/cli/commands/`):
1. `add.py`
2. `capture.py`
3. `compact.py`
4. `hooks.py`
5. `memory.py`
6. `mcp.py`
7. `queue_cmd.py`
8. `show.py`
9. `stale.py`
10. `summarize.py`
11. `sync.py`

**DO NOT delete `index.py`** — it is kept as a hidden command (`recall index --force`) registered in `__init__.py` by Plan 01.

Before deleting, verify that `src/cli/__init__.py` does NOT import any of these 11 files (grep for each module name). If an import is still present in `__init__.py`, abort and report — Plan 01 must have been incomplete.

After deleting, verify `src/cli/__init__.py` imports cleanly:
```bash
python -c "import src.cli; print('ok')"
```
  </action>
  <verify>
    <automated>
      python -c "import src.cli; print('import ok')" 2>&amp;1 | head -5 &amp;&amp;
      ls /home/tasostilsi/Development/Projects/graphiti-knowledge-graph/src/cli/commands/ | grep -E "^add\.py|^capture\.py|^compact\.py|^hooks\.py|^memory\.py|^mcp\.py|^queue_cmd\.py|^show\.py|^stale\.py|^summarize\.py|^sync\.py" | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/add.py` does NOT exist
    - `src/cli/commands/capture.py` does NOT exist
    - `src/cli/commands/compact.py` does NOT exist
    - `src/cli/commands/hooks.py` does NOT exist
    - `src/cli/commands/memory.py` does NOT exist
    - `src/cli/commands/mcp.py` does NOT exist
    - `src/cli/commands/queue_cmd.py` does NOT exist
    - `src/cli/commands/show.py` does NOT exist
    - `src/cli/commands/stale.py` does NOT exist
    - `src/cli/commands/summarize.py` does NOT exist
    - `src/cli/commands/sync.py` does NOT exist
    - `src/cli/commands/index.py` DOES exist (kept as hidden command)
    - `python -c "import src.cli; print('ok')"` prints `ok` (no import errors)
    - `ls src/cli/commands/` shows: `__init__.py config.py delete.py health.py index.py init_cmd.py list_cmd.py note_cmd.py pin.py search.py ui.py` (and unpin.py if separate)
  </acceptance_criteria>
  <done>All 11 command files deleted; index.py kept; import src.cli succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Update all hook scripts to use recall binary; fix session_start to use GitIndexer directly</name>
  <files>
    src/hooks/session_start.py,
    src/hooks/capture_entry.py,
    src/hooks/session_stop.py,
    src/hooks/inject_context.py,
    src/hooks/manager.py,
    src/hooks/installer.py
  </files>
  <read_first>
    - src/hooks/session_start.py (current: subprocess [_GRAPHITI_CLI, "sync"] — replace with GitIndexer direct call)
    - src/hooks/capture_entry.py (grep for "graphiti" binary references)
    - src/hooks/session_stop.py (grep for "graphiti" binary references)
    - src/hooks/inject_context.py (grep for "graphiti" binary references)
    - src/hooks/manager.py (line 15: _GRAPHITI_CLI constant — rename to _RECALL_CLI)
    - src/hooks/installer.py (install_global_hooks function — check for graphiti binary references)
  </read_first>
  <action>
**`src/hooks/session_start.py` — replace subprocess sync with direct GitIndexer call:**

`sync` is fully deleted — there is no `recall sync` to subprocess to. Instead, call `GitIndexer` directly in Python, which is simpler and more robust for a hook script context (no subprocess, no PATH issues, no binary name dependency).

Remove the `_GRAPHITI_CLI` constant entirely. Replace `_run_sync` with a direct Python call:

Current `_GRAPHITI_CLI` constant (line 28) — DELETE this line:
```python
_GRAPHITI_CLI = str(Path(sys.executable).parent / "graphiti")
```

Current `_run_sync` function — REPLACE with:
```python
def _run_sync(project_root: Path) -> None:
    """Run incremental git index directly via GitIndexer. Partial progress preserved by state file."""
    try:
        # Fix sys.path for import (CWD undefined when Claude Code calls hooks)
        # _PROJECT_PKG_ROOT is already inserted at module load time above
        from src.indexer import GitIndexer
        GitIndexer(project_root=project_root).run(full=False)
    except Exception as e:
        logger.warning("session_start_sync_error", error=str(e))
```

Update the module docstring (line 5): change "Calls graphiti sync with a 4.5s hard timeout" to "Calls GitIndexer directly for incremental git sync (no subprocess — more robust in hook context)."

Remove the `SYNC_TIMEOUT_SECONDS = 4.5` constant (no longer needed — no subprocess timeout).

Remove the `import subprocess` line (no longer needed).

Update the `FileNotFoundError` handler in the old `_run_sync` — it no longer applies. The new `_run_sync` only has one `except Exception` block.

**`src/hooks/capture_entry.py` — find and replace graphiti binary references:**

Read the file and find all references to the `graphiti` binary (subprocess calls or path construction). Replace each `"graphiti"` binary name with `"recall"`. If the file uses a constant like `_GRAPHITI_CLI`, rename it to `_RECALL_CLI` and update all usages.

**`src/hooks/session_stop.py` — find and replace graphiti binary references:**

Read the file and find all references to the `graphiti` binary. Replace each `"graphiti"` binary name with `"recall"`. Apply the same constant renaming pattern if applicable.

**`src/hooks/inject_context.py` — find and replace graphiti binary references:**

Read the file and find all references to the `graphiti` binary. Replace each `"graphiti"` binary name with `"recall"`. Apply the same constant renaming pattern if applicable.

**`src/hooks/manager.py` — rename _GRAPHITI_CLI constant:**

Line 15 changes from:
```python
_GRAPHITI_CLI = str(Path(sys.executable).parent / "graphiti")
```
To:
```python
_RECALL_CLI = str(Path(sys.executable).parent / "recall")
```

Update all references to `_GRAPHITI_CLI` in the file → `_RECALL_CLI`. The manager uses this in subprocess calls — update those subprocess args:
```python
[_RECALL_CLI, "config", ...]
```

Note: the `get_hooks_enabled()` / `set_hooks_enabled()` calls using `config get hooks.enabled` are vestigial (hooks are always enabled in v2.0 via global hook scripts). Update the constant name only; keep try/except wrappers — these calls will gracefully fail/default as before. Add a comment: `# NOTE: get/set_hooks_enabled() calls are vestigial v1.x code — hooks are always enabled in v2.0`.

**`src/hooks/installer.py` — update legacy hook command reference:**

The `install_global_hooks()` function builds command strings that call `{python_exe} {hook_script}` (not the graphiti/recall binary) — no changes needed there.

However, `install_claude_hook()` (legacy project-local Stop hook) contains:
```python
"command": 'graphiti capture --auto --transcript-path "$transcript_path" --session-id "$session_id"',
```

Update to:
```python
"command": 'recall note "$transcript_path"',
```

Add comment above the function: `# NOTE: install_claude_hook() is legacy (v1.x project-local hook). recall init uses install_global_hooks() instead. Kept for backward compatibility.`

Also update `is_claude_hook_installed` check (grep for `"graphiti capture"` string):
```python
if isinstance(h, dict) and "graphiti capture" in h.get("command", ""):
```
Change to:
```python
if isinstance(h, dict) and ("graphiti capture" in h.get("command", "") or "recall note" in h.get("command", "")):
```

And in `uninstall_claude_hook`, update the analogous check from `"graphiti capture"` to include `"recall note"`.
  </action>
  <verify>
    <automated>
      grep -n "_RECALL_CLI\|_GRAPHITI_CLI" /home/tasostilsi/Development/Projects/graphiti-knowledge-graph/src/hooks/session_start.py /home/tasostilsi/Development/Projects/graphiti-knowledge-graph/src/hooks/manager.py &amp;&amp;
      grep -n "GitIndexer" /home/tasostilsi/Development/Projects/graphiti-knowledge-graph/src/hooks/session_start.py &amp;&amp;
      grep -rn "graphiti" /home/tasostilsi/Development/Projects/graphiti-knowledge-graph/src/hooks/capture_entry.py /home/tasostilsi/Development/Projects/graphiti-knowledge-graph/src/hooks/session_stop.py /home/tasostilsi/Development/Projects/graphiti-knowledge-graph/src/hooks/inject_context.py | grep -v "graphiti-knowledge-graph\|graphiti-core\|graphiti_core\|# " | head -10 &amp;&amp;
      python -c "from src.hooks.session_start import main; from src.hooks.manager import get_hooks_enabled; print('imports ok')" 2>&amp;1 | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/session_start.py` does NOT contain `_GRAPHITI_CLI` or `subprocess` import
    - `src/hooks/session_start.py` contains `from src.indexer import GitIndexer` inside `_run_sync`
    - `src/hooks/session_start.py` contains `GitIndexer(project_root=project_root).run(full=False)`
    - `src/hooks/capture_entry.py` contains no references to `graphiti` binary (subprocess args or path construction)
    - `src/hooks/session_stop.py` contains no references to `graphiti` binary (subprocess args or path construction)
    - `src/hooks/inject_context.py` contains no references to `graphiti` binary (subprocess args or path construction)
    - `src/hooks/manager.py` contains `_RECALL_CLI = str(Path(sys.executable).parent / "recall")` (exact string)
    - `src/hooks/manager.py` does NOT contain `_GRAPHITI_CLI`
    - `src/hooks/installer.py` legacy hook command contains `recall note` (not `graphiti capture`)
    - `python -c "from src.hooks.session_start import main; print('ok')"` prints `ok`
    - `python -c "from src.hooks.manager import get_hooks_enabled; print('ok')"` prints `ok`
  </acceptance_criteria>
  <done>session_start.py uses GitIndexer directly (no subprocess); all four hook scripts have no graphiti binary refs; manager.py uses _RECALL_CLI; installer.py legacy command updated</done>
</task>

</tasks>

<verification>
After both tasks:
1. `python -c "import src.cli; print('ok')"` succeeds (no missing imports)
2. `python -c "from src.hooks.session_start import main; print('ok')"` succeeds
3. `ls src/cli/commands/` does not list: add.py, capture.py, compact.py, hooks.py, memory.py, mcp.py, queue_cmd.py, show.py, stale.py, summarize.py, sync.py
4. `ls src/cli/commands/` DOES list: index.py (kept as hidden command)
5. `grep "_GRAPHITI_CLI" src/hooks/session_start.py src/hooks/manager.py` returns no matches
6. `grep "_RECALL_CLI" src/hooks/manager.py` returns at least 1 match
7. `grep "GitIndexer" src/hooks/session_start.py` returns at least 1 match
8. `grep -l "graphiti" src/hooks/capture_entry.py src/hooks/session_stop.py src/hooks/inject_context.py` returns no matches (for binary references — not counting comments or package name strings)
</verification>

<success_criteria>
- 11 command files deleted (index.py kept), `import src.cli` succeeds
- All four Phase 15 hook scripts (`session_start.py`, `capture_entry.py`, `session_stop.py`, `inject_context.py`) call `recall` not `graphiti`
- `session_start.py` calls `GitIndexer` directly — no subprocess, no binary dependency
- `manager.py` uses `_RECALL_CLI` / `recall` binary
- `installer.py` legacy stop hook command updated from `graphiti capture` to `recall note`
</success_criteria>

<output>
After completion, create `.planning/phases/16-rename-cli-consolidation/16-03-SUMMARY.md`
</output>
