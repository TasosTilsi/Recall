---
phase: 23-nyquist-compliance-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-db-migration/12-VALIDATION.md
  - .planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md
  - .planning/phases/14-graph-ui-redesign/14-VALIDATION.md
  - .planning/phases/15-local-memory-system/15-VALIDATION.md
  - .planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 12 VALIDATION.md has nyquist_compliant: true with all File Exists columns showing exists"
    - "Phase 13 VALIDATION.md references test_llm_config.py and test_provider.py (not phantom files)"
    - "Phase 15 VALIDATION.md references test_hooks_phase15.py (not tests/test_hooks/ subdirectory)"
    - "Phase 19 VALIDATION.md references test_phase19_integration.py (not phantom test_phase19.py)"
    - "Phase 14 VALIDATION.md has nyquist_compliant: true with wave_0_complete: false (forward contract per D-02)"
  artifacts:
    - path: ".planning/phases/12-db-migration/12-VALIDATION.md"
      provides: "Compliant validation for Phase 12"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md"
      provides: "Compliant validation for Phase 13"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/14-graph-ui-redesign/14-VALIDATION.md"
      provides: "Compliant validation for Phase 14"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/15-local-memory-system/15-VALIDATION.md"
      provides: "Compliant validation for Phase 15"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md"
      provides: "Compliant validation for Phase 19"
      contains: "nyquist_compliant: true"
  key_links: []
---

<objective>
Update the 5 existing VALIDATION.md stubs for phases 12, 13, 14, 15, and 19 to achieve `nyquist_compliant: true`.

Purpose: These stubs exist but have `nyquist_compliant: false`, stale File Exists columns (`W0` for files that now exist), and phantom test file references. Each must be reviewed per D-04/D-05 and corrected before flipping.

Output: 5 updated VALIDATION.md files, each with `nyquist_compliant: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-nyquist-compliance-sweep/23-CONTEXT.md
@.planning/phases/23-nyquist-compliance-sweep/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update VALIDATION.md stubs for phases 12, 13, 15 (completed code phases)</name>
  <files>
    .planning/phases/12-db-migration/12-VALIDATION.md
    .planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md
    .planning/phases/15-local-memory-system/15-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/12-db-migration/12-VALIDATION.md
    .planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md
    .planning/phases/15-local-memory-system/15-VALIDATION.md
    .planning/phases/23-nyquist-compliance-sweep/23-RESEARCH.md
    .planning/phases/23-nyquist-compliance-sweep/23-CONTEXT.md
  </read_first>
  <action>
Update each stub in-place following the three-step upgrade pattern from 23-RESEARCH.md. Specific changes per file:

**Phase 12** (`.planning/phases/12-db-migration/12-VALIDATION.md`):
Per D-04 and D-05, tests exist and pass (confirmed in research). Apply these exact changes:
- Frontmatter: set `nyquist_compliant: true`, `wave_0_complete: true`
- Per-Task Verification Map: change ALL `File Exists` cells from `❌ W0` to `✅ exists`; change ALL `Status` cells from `⬜ pending` to `✅ green`
- Wave 0 Requirements: check all 4 boxes (`[x]` instead of `[ ]`)
- Validation Sign-Off: check all 6 boxes; set Approval to `passed — all tests exist and pass (435 passed, 1 skipped, 2026-04-04)`

**Phase 13** (`.planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md`):
Per D-04, D-05, and D-06: update in-place in the legacy directory (do NOT create a new directory). Apply these exact changes:
- Frontmatter: set `nyquist_compliant: true`, `wave_0_complete: true`
- Add a note after the H1 heading: `> **Note:** Directory name is a legacy artifact from old phase numbering. Phase 13 = Multi-Provider LLM per ROADMAP.md.`
- Per-Task Verification Map: replace ALL references to `tests/test_llm_provider.py` with `tests/test_llm_config.py`; replace ALL references to `tests/test_llm_startup.py` with `tests/test_provider.py`; change ALL `File Exists` cells to `✅ exists`; change ALL `Status` cells to `✅ green`
- Wave 0 Requirements: remove entries for phantom `test_llm_provider.py` and `test_llm_startup.py`; add checked entries for `tests/test_llm_config.py` and `tests/test_provider.py`; check all boxes
- Validation Sign-Off: check all 6 boxes; set Approval to `passed — all tests exist and pass (test_llm_config 24 passed, test_provider passes)`

**Phase 15** (`.planning/phases/15-local-memory-system/15-VALIDATION.md`):
Per D-04, D-05: the stub references `tests/test_hooks/` subdirectory which does not exist. Actual tests are in `tests/test_hooks_phase15.py` (16 tests, all pass) and `tests/test_global_hooks_installer.py`. Apply these exact changes:
- Frontmatter: set `nyquist_compliant: true`, `wave_0_complete: true`
- Per-Task Verification Map: replace ALL `tests/test_hooks/*.py` paths with `tests/test_hooks_phase15.py`; replace `tests/test_hooks/test_installer.py` with `tests/test_global_hooks_installer.py`; update ALL automated commands accordingly; change ALL `File Exists` cells to `✅ exists`; change ALL `Status` cells to `✅ green`
- Sampling Rate: change `pytest tests/test_hooks/ -x -q` to `pytest tests/test_hooks_phase15.py tests/test_global_hooks_installer.py -x -q`
- Wave 0 Requirements: remove all `tests/test_hooks/` subdirectory entries; add checked entries for actual files; check all boxes
- Validation Sign-Off: check all 6 boxes; set Approval to `passed — all tests exist and pass (16 + installer tests pass)`
  </action>
  <verify>
    <automated>cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && grep -c 'nyquist_compliant: true' .planning/phases/12-db-migration/12-VALIDATION.md .planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md .planning/phases/15-local-memory-system/15-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'nyquist_compliant: true' .planning/phases/12-db-migration/12-VALIDATION.md` exits 0
    - `grep 'nyquist_compliant: true' .planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md` exits 0
    - `grep 'nyquist_compliant: true' .planning/phases/15-local-memory-system/15-VALIDATION.md` exits 0
    - `grep 'wave_0_complete: true' .planning/phases/12-db-migration/12-VALIDATION.md` exits 0
    - `grep 'wave_0_complete: true' .planning/phases/15-local-memory-system/15-VALIDATION.md` exits 0
    - `grep -c 'test_llm_provider.py' .planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md` returns 0 (phantom removed)
    - `grep -c 'test_llm_startup.py' .planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md` returns 0 (phantom removed)
    - `grep 'test_llm_config.py' .planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md` exits 0 (real file referenced)
    - `grep -c 'tests/test_hooks/' .planning/phases/15-local-memory-system/15-VALIDATION.md` returns 0 (phantom subdir removed)
    - `grep 'test_hooks_phase15.py' .planning/phases/15-local-memory-system/15-VALIDATION.md` exits 0 (real file referenced)
    - `grep 'legacy artifact' .planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md` exits 0 (D-06 note present)
  </acceptance_criteria>
  <done>Phases 12, 13, 15 VALIDATION.md files updated with correct test references, all File Exists columns show exists, all Status columns show green, nyquist_compliant: true, wave_0_complete: true, Sign-Off complete with Approval set to passed.</done>
</task>

<task type="auto">
  <name>Task 2: Update VALIDATION.md stubs for phases 14 and 19</name>
  <files>
    .planning/phases/14-graph-ui-redesign/14-VALIDATION.md
    .planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/14-graph-ui-redesign/14-VALIDATION.md
    .planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md
    .planning/phases/23-nyquist-compliance-sweep/23-RESEARCH.md
    .planning/phases/23-nyquist-compliance-sweep/23-CONTEXT.md
  </read_first>
  <action>
Update each stub in-place. Specific changes per file:

**Phase 14** (`.planning/phases/14-graph-ui-redesign/14-VALIDATION.md`):
Per D-02: forward-looking contract is sufficient. Phase 14 is "In Progress" — compliance is a planning artifact.
- Frontmatter: set `nyquist_compliant: true`; keep `wave_0_complete: false` (vitest infrastructure not fully set up)
- Per-Task Verification Map: change `test_graph_service_ui.py` File Exists cell from `❌ W0` to `✅ exists` (this file does exist); leave vitest/frontend entries as `❌ W0` if they reference files that do not exist; leave all Status cells as `⬜ pending` (forward contract)
- Wave 0 Requirements: leave unchecked items for vitest stubs that do not yet exist; check items for files that do exist
- Validation Sign-Off: check 5 of 6 items (leave Wave 0 item unchecked); set Approval to `forward-contract — see D-02`

**Phase 19** (`.planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md`):
Per D-04 and D-05: tests exist and pass. The stub references phantom `tests/test_phase19.py` which does not exist. Actual coverage is in `tests/test_phase19_integration.py` (16 tests, all pass).
- Frontmatter: set `nyquist_compliant: true`, `wave_0_complete: true`
- Per-Task Verification Map: replace ALL references to `tests/test_phase19.py` with `tests/test_phase19_integration.py`; update automated commands accordingly; change File Exists cells to `✅ exists` for real files; change Status cells to `✅ green`
- Sampling Rate quick run command: change `pytest tests/test_phase19.py -x -q` to `pytest tests/test_phase19_integration.py -x -q`
- Wave 0 Requirements: remove phantom `tests/test_phase19.py` entry; add checked entry for `tests/test_phase19_integration.py`; check all boxes
- Validation Sign-Off: check all 6 boxes; set Approval to `passed — test_phase19_integration.py 16 passed`
  </action>
  <verify>
    <automated>cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && grep -c 'nyquist_compliant: true' .planning/phases/14-graph-ui-redesign/14-VALIDATION.md .planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'nyquist_compliant: true' .planning/phases/14-graph-ui-redesign/14-VALIDATION.md` exits 0
    - `grep 'nyquist_compliant: true' .planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md` exits 0
    - `grep 'wave_0_complete: false' .planning/phases/14-graph-ui-redesign/14-VALIDATION.md` exits 0 (kept false for forward contract)
    - `grep 'wave_0_complete: true' .planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md` exits 0
    - `grep 'forward-contract' .planning/phases/14-graph-ui-redesign/14-VALIDATION.md` exits 0 (D-02 approval note)
    - `grep -c 'test_phase19\.py' .planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md` returns 0 (phantom removed -- note: test_phase19_integration.py contains the substring but is the correct file)
    - `grep 'test_phase19_integration.py' .planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md` exits 0 (real file referenced)
  </acceptance_criteria>
  <done>Phases 14 and 19 VALIDATION.md files updated: Phase 14 marked as forward-contract compliant (D-02), Phase 19 corrected to reference real test files. Both have nyquist_compliant: true.</done>
</task>

</tasks>

<verification>
All 5 existing stubs updated with correct test references and nyquist_compliant: true.

```bash
grep -l 'nyquist_compliant: true' .planning/phases/12-db-migration/12-VALIDATION.md .planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md .planning/phases/14-graph-ui-redesign/14-VALIDATION.md .planning/phases/15-local-memory-system/15-VALIDATION.md .planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md
```
Must return all 5 paths.
</verification>

<success_criteria>
- 5/5 stubs have `nyquist_compliant: true` in frontmatter
- No phantom test file references remain (test_llm_provider.py, test_llm_startup.py, tests/test_hooks/, test_phase19.py)
- Phase 13 has legacy directory note per D-06
- Phase 14 has `wave_0_complete: false` with forward-contract approval per D-02
- All completed phase stubs (12, 13, 15, 19) have `wave_0_complete: true`
</success_criteria>

<output>
After completion, create `.planning/phases/23-nyquist-compliance-sweep/23-01-SUMMARY.md`
</output>
