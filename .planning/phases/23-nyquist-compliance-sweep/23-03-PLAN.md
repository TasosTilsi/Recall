---
phase: 23-nyquist-compliance-sweep
plan: 03
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - .planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md
  - .planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 20 VALIDATION.md exists with nyquist_compliant: true referencing test_claude_cli_client.py, test_indexer_batch.py, test_hooks_phase20.py"
    - "Phase 21 VALIDATION.md exists with nyquist_compliant: true referencing test_extraction_phase21.py"
    - "All 10 v2.0 phases (12-21) have nyquist_compliant: true in their VALIDATION.md"
  artifacts:
    - path: ".planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md"
      provides: "Compliant validation for Phase 20"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md"
      provides: "Compliant validation for Phase 21"
      contains: "nyquist_compliant: true"
  key_links: []
---

<objective>
Create VALIDATION.md for phases 20 and 21 (completed code phases with full test coverage), then run a final sweep confirming all 10 v2.0 phases have `nyquist_compliant: true`.

Purpose: Phases 20 and 21 are complete with passing tests but have no VALIDATION.md. After creating these, the 10/10 compliance goal is met.

Output: 2 new VALIDATION.md files + confirmation that `grep -r 'nyquist_compliant: true' .planning/phases/*/[0-9]*-VALIDATION.md | wc -l` returns 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-nyquist-compliance-sweep/23-CONTEXT.md
@.planning/phases/23-nyquist-compliance-sweep/23-RESEARCH.md
@.planning/phases/12-db-migration/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VALIDATION.md for phases 20 and 21 (completed code phases)</name>
  <files>
    .planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md
    .planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/12-db-migration/12-VALIDATION.md
    .planning/phases/23-nyquist-compliance-sweep/23-RESEARCH.md
    .planning/phases/23-nyquist-compliance-sweep/23-CONTEXT.md
  </read_first>
  <action>
Create both files using the Phase 12 template format. Per D-01, these are completed code phases with tests that exist and pass.

**Phase 20** (`.planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md`):

Frontmatter:
```yaml
phase: 20
slug: fast-indexing-claude-cli-batch-fts
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-04
```

Content specifics:
- H1: `# Phase 20 — Validation Strategy`
- Standard blockquote: `> Per-phase validation contract for feedback sampling during execution.`
- Test Infrastructure table: Framework = `pytest 7.x`, Config = `pyproject.toml`, Quick run = `pytest tests/test_claude_cli_client.py tests/test_indexer_batch.py tests/test_hooks_phase20.py -x -q`, Full suite = `pytest tests/ -v`, Runtime = `~30 seconds`
- Sampling Rate: standard 4 bullets using the quick run command above
- Per-Task Verification Map: 5 rows (one per plan 20-01 through 20-05), map requirements PERF-01 through PERF-05 as follows:
  - 20-01: PERF-02 (ClaudeCliLLMClient), Test Type = unit, Command = `pytest tests/test_claude_cli_client.py -x -q`, File Exists = `✅ exists`, Status = `✅ green`
  - 20-02: PERF-01 (batch extraction + semaphore), Test Type = unit, Command = `pytest tests/test_indexer_batch.py -x -q`, File Exists = `✅ exists`, Status = `✅ green`
  - 20-03: PERF-05 (FTS-first inject_context), Test Type = unit, Command = `pytest tests/test_hooks_phase20.py -x -q`, File Exists = `✅ exists`, Status = `✅ green`
  - 20-04: PERF-03 (session_stop claude CLI), Test Type = unit, Command = `pytest tests/test_hooks_phase20.py -x -q`, File Exists = `✅ exists`, Status = `✅ green`
  - 20-05: PERF-01/02/03/04/05 (full test suite), Test Type = integration, Command = `pytest tests/test_claude_cli_client.py tests/test_indexer_batch.py tests/test_hooks_phase20.py -x -q`, File Exists = `✅ exists`, Status = `✅ green`
- Wave 0 Requirements: `None — all referenced tests exist. Verified 2026-04-04: 24 tests pass (8 + 6 + 9 + 1 skipped).`
- Manual-Only Verifications: one row — `30-commit indexing under 2 minutes` | PERF-01 | Requires real git repo + Claude CLI | Covered by 20-VERIFICATION.md timing proof
- Validation Sign-Off: all 6 items checked; Approval = `passed — all tests exist and pass per 20-VERIFICATION.md (status: passed, 5/5 verified)`

**Phase 21** (`.planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md`):

Frontmatter:
```yaml
phase: 21
slug: knowledge-quality-uplift
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-04
```

Content specifics:
- H1: `# Phase 21 — Validation Strategy`
- Standard blockquote: `> Per-phase validation contract for feedback sampling during execution.`
- Test Infrastructure table: Framework = `pytest 7.x (Python) + npx tsx (TypeScript)`, Config = `pyproject.toml`, Quick run = `pytest tests/test_extraction_phase21.py -x -q`, Full suite = `pytest tests/ -x -q && cd ui && npx tsx src/lib/parseCodeBlockMeta.test.ts`, Runtime = `~30 seconds`
- Sampling Rate: standard 4 bullets using quick run command
- Per-Task Verification Map: 3 rows (one per plan 21-01 through 21-03), map requirements SC-1 through SC-5:
  - 21-01: SC-1/SC-2/SC-3/SC-4 (extraction prompt + code blocks), Test Type = unit, Command = `pytest tests/test_extraction_phase21.py -x -q`, File Exists = `✅ exists`, Status = `✅ green`
  - 21-02: SC-5 (UI entity cards), Test Type = unit, Command = `cd ui && npx tsx src/lib/parseCodeBlockMeta.test.ts`, File Exists = `✅ exists`, Status = `✅ green`
  - 21-03: SC-1/SC-2/SC-3/SC-4/SC-5 (full test suite), Test Type = integration, Command = `pytest tests/test_extraction_phase21.py -x -q`, File Exists = `✅ exists`, Status = `✅ green`
- Wave 0 Requirements: `None — all referenced tests exist. Verified 2026-04-04: 10 Python tests pass, 8 TypeScript tests pass.`
- Manual-Only Verifications: one row — `Entity cards display structured context in UI` | SC-5 | Visual inspection | Covered by 21-VERIFICATION.md human-verify checkpoint
- Validation Sign-Off: all 6 items checked; Approval = `passed — all tests exist and pass per 21-VERIFICATION.md (status: passed, 5/5 verified)`
  </action>
  <verify>
    <automated>cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && test -f .planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md && test -f .planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md && grep 'nyquist_compliant: true' .planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md && grep 'nyquist_compliant: true' .planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md` exits 0
    - `test -f .planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md` exits 0
    - `grep 'nyquist_compliant: true' .planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md` exits 0
    - `grep 'nyquist_compliant: true' .planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md` exits 0
    - `grep 'wave_0_complete: true' .planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md` exits 0
    - `grep 'wave_0_complete: true' .planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md` exits 0
    - `grep 'test_claude_cli_client.py' .planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md` exits 0
    - `grep 'test_indexer_batch.py' .planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md` exits 0
    - `grep 'test_hooks_phase20.py' .planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md` exits 0
    - `grep 'test_extraction_phase21.py' .planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md` exits 0
    - `grep 'parseCodeBlockMeta.test.ts' .planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md` exits 0
  </acceptance_criteria>
  <done>Phases 20 and 21 VALIDATION.md created with nyquist_compliant: true, correct test file references, all File Exists showing exists, all Status showing green.</done>
</task>

<task type="auto">
  <name>Task 2: Final sweep — confirm 10/10 nyquist_compliant: true across all v2.0 phases</name>
  <files></files>
  <read_first>
    .planning/phases/23-nyquist-compliance-sweep/23-CONTEXT.md
  </read_first>
  <action>
Run the final compliance sweep command to confirm all 10 v2.0 phases (12–21) have `nyquist_compliant: true` in their VALIDATION.md. The expected files are:

1. `.planning/phases/12-db-migration/12-VALIDATION.md`
2. `.planning/phases/13-graph-ui-redesign-shadcn-ui-dual-view-table-and-graph/13-VALIDATION.md`
3. `.planning/phases/14-graph-ui-redesign/14-VALIDATION.md`
4. `.planning/phases/15-local-memory-system/15-VALIDATION.md`
5. `.planning/phases/16-rename-cli-consolidation/16-VALIDATION.md`
6. `.planning/phases/17-fix-stale-binary-references/17-VALIDATION.md`
7. `.planning/phases/18-formal-verification-phases-14-16/18-VALIDATION.md`
8. `.planning/phases/19-wire-ui-03-retention-filter/19-VALIDATION.md`
9. `.planning/phases/20-fast-indexing-claude-cli-batch-fts/20-VALIDATION.md`
10. `.planning/phases/21-knowledge-quality-uplift/21-VALIDATION.md`

Run: `grep -rl 'nyquist_compliant: true' .planning/phases/*/[0-9]*-VALIDATION.md | grep -v '23-' | wc -l`

The result MUST be exactly `10`. If fewer than 10, identify which phase is missing and report the gap.

Also run `grep -rL 'nyquist_compliant: true' .planning/phases/*/[0-9]*-VALIDATION.md | grep -v '23-'` to find any non-compliant files (should return empty).
  </action>
  <verify>
    <automated>cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && COUNT=$(grep -rl 'nyquist_compliant: true' .planning/phases/*/[0-9]*-VALIDATION.md | grep -v '23-' | wc -l) && test "$COUNT" -eq 10 && echo "PASS: 10/10 compliant"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl 'nyquist_compliant: true' .planning/phases/*/[0-9]*-VALIDATION.md | grep -v '23-' | wc -l` outputs exactly `10`
    - `grep -rL 'nyquist_compliant: true' .planning/phases/*/[0-9]*-VALIDATION.md | grep -v '23-'` outputs nothing (empty)
  </acceptance_criteria>
  <done>All 10 v2.0 phases (12–21) confirmed as nyquist_compliant: true. The systemic validation gap is closed. Milestone is ready for /gsd:complete-milestone.</done>
</task>

</tasks>

<verification>
Final compliance gate:

```bash
COUNT=$(grep -rl 'nyquist_compliant: true' .planning/phases/*/[0-9]*-VALIDATION.md | grep -v '23-' | wc -l)
echo "$COUNT/10 phases compliant"
test "$COUNT" -eq 10
```

Must output `10/10 phases compliant` and exit 0.
</verification>

<success_criteria>
- Phases 20 and 21 have new VALIDATION.md files with `nyquist_compliant: true`
- Phase 20 references test_claude_cli_client.py, test_indexer_batch.py, test_hooks_phase20.py
- Phase 21 references test_extraction_phase21.py and parseCodeBlockMeta.test.ts
- Final sweep confirms 10/10 compliant (phases 12–21, excluding 22 and 23)
- No phantom test file references in any VALIDATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/23-nyquist-compliance-sweep/23-03-SUMMARY.md`
</output>
