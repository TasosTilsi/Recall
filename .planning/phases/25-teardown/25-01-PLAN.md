---
phase: 25-teardown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/
  - src/queue/
  - src/retention/
  - src/capture/
  - src/storage/
  - src/graph/
  - src/security/
  - src/llm/
  - src/models/
  - src/gitops/
  - src/config/
  - src/graphiti_knowledge_graph.egg-info/
  - tests/
  - scripts/
  - src/ui_server/app.py
  - src/ui_server/routes.py
  - src/db/__init__.py
  - src/extractor/__init__.py
  - src/config.py
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "No legacy module directories exist under src/"
    - "tests/ and scripts/ directories are gone"
    - "ui_server/ has no imports from deleted modules"
    - "src/db/ and src/extractor/ skeleton directories exist"
  artifacts:
    - path: "src/db/__init__.py"
      provides: "Empty skeleton package for Phase 26"
    - path: "src/extractor/__init__.py"
      provides: "Empty skeleton package for Phase 28"
    - path: "src/config.py"
      provides: "Empty config module placeholder for Phase 27"
  key_links:
    - from: "src/ui_server/app.py"
      to: "deleted src/graph/service.py"
      via: "import removed or stubbed"
      pattern: "no.*from src\\.graph"
    - from: "src/ui_server/routes.py"
      to: "deleted src/retention, src/llm, src/cli.utils"
      via: "imports removed or stubbed"
      pattern: "no.*from src\\.retention|from src\\.llm|from src\\.cli\\.utils"
---

<objective>
Delete all legacy modules, tests, and scripts. Fix ui_server imports that reference deleted modules. Create skeleton directories for the new layout.

Purpose: Strip the codebase of all v2.0 components that are being replaced, leaving only the surviving ui_server/ (with fixed imports) and empty skeleton packages for downstream phases.

Output: A src/ tree with only: __init__.py, cli/ (legacy, will be gutted in Plan 02), mcp_server/ (legacy, gutted in Plan 02), ui_server/ (fixed imports), indexer/ (existing, kept), db/ (new skeleton), extractor/ (new skeleton), config.py (new skeleton).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-teardown/25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all legacy modules, tests, scripts, and egg-info</name>
  <files>
    src/hooks/
    src/queue/
    src/retention/
    src/capture/
    src/storage/
    src/graph/
    src/security/
    src/llm/
    src/models/
    src/gitops/
    src/config/
    src/graphiti_knowledge_graph.egg-info/
    tests/
    scripts/
  </files>
  <read_first>
    - src/ui_server/app.py — check imports from modules being deleted (src.graph.service)
    - src/ui_server/routes.py — check imports from modules being deleted (src.cli.utils, src.retention, src.llm.config)
    - src/__init__.py — confirm it exists and see current content
  </read_first>
  <action>
    1. Delete these directories entirely (rm -rf):
       - src/hooks/
       - src/queue/
       - src/retention/
       - src/capture/
       - src/storage/
       - src/graph/
       - src/security/
       - src/llm/
       - src/models/
       - src/gitops/
       - src/config/          (the DIRECTORY — not src/config.py which is created in Task 2)
       - src/graphiti_knowledge_graph.egg-info/
       - tests/
       - scripts/

    2. Fix src/ui_server/app.py — remove or comment out the broken import:
       - DELETE the line: `from src.graph.service import GraphService`
       - Find all uses of `GraphService` in app.py and replace with `None` or remove the code block.
         Specifically, if there is a `graph_service` global or assignment, set it to `None`.
         If `create_app()` or similar function instantiates GraphService, replace with a pass or comment
         `# TODO: Phase 31 — reconnect to new SQLite-backed service`.

    3. Fix src/ui_server/routes.py — remove or neutralize broken imports:
       - The file has lazy imports inside functions (lines ~40, ~182-183). Find and fix these:
         a. `from src.cli.utils import resolve_scope` — replace the function body that uses this
            with a stub: `scope = "default"` or wrap in try/except that returns a fallback.
            Preferred: wrap the import in try/except ImportError, set resolve_scope = lambda: "default"
         b. `from src.retention import get_retention_manager` — same pattern: wrap in try/except ImportError,
            and if the import fails, have the code path return an empty dict or skip retention data.
         c. `from src.llm.config import load_config` — same pattern: wrap in try/except ImportError,
            return a default/empty config dict if import fails.

    4. Keep src/__init__.py as-is (just the `# Make src a package` comment).

    Per D1: ui_server/ carries forward unchanged except for these import fixes.
    Per D4: tests/ and scripts/ deleted entirely, no placeholders.
    Per ARCH-01: All session-capture components removed (hooks, queue, retention, capture).
    Per ARCH-02: graphiti-core related modules removed (graph/, storage/, llm/).
  </action>
  <verify>
    <automated>cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && python -c "
import os
legacy = ['hooks','queue','retention','capture','storage','graph','security','llm','models','gitops']
for d in legacy:
    assert not os.path.exists(f'src/{d}'), f'Legacy dir src/{d} still exists'
assert not os.path.exists('src/config'), 'src/config/ dir still exists'
assert not os.path.exists('src/graphiti_knowledge_graph.egg-info')
assert not os.path.exists('tests')
assert not os.path.exists('scripts')
print('All legacy modules deleted')
" && python -c "
# Verify ui_server has no broken top-level imports
import ast, sys
for f in ['src/ui_server/app.py', 'src/ui_server/routes.py']:
    with open(f) as fh:
        tree = ast.parse(fh.read())
    for node in ast.walk(tree):
        if isinstance(node, ast.ImportFrom) and node.module:
            for bad in ['src.graph', 'src.retention', 'src.llm', 'src.cli.utils']:
                if node.module.startswith(bad):
                    print(f'FAIL: {f} has top-level import from {node.module}')
                    sys.exit(1)
print('ui_server imports clean')
"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `ls src/` does NOT contain: hooks, queue, retention, capture, storage, graph, security, llm, models, gitops, config (directory)
    - [ ] `ls src/graphiti_knowledge_graph.egg-info` returns "No such file or directory"
    - [ ] `ls tests/` returns "No such file or directory"
    - [ ] `ls scripts/` returns "No such file or directory"
    - [ ] `grep -n "from src.graph" src/ui_server/app.py` returns no matches (or only inside try/except)
    - [ ] `grep -n "from src.retention" src/ui_server/routes.py` returns no top-level matches (only inside try/except)
    - [ ] `grep -n "from src.llm" src/ui_server/routes.py` returns no top-level matches (only inside try/except)
    - [ ] `grep -n "from src.cli.utils" src/ui_server/routes.py` returns no top-level matches (only inside try/except)
  </acceptance_criteria>
  <done>All 14 legacy directories plus tests/ and scripts/ are deleted. ui_server/ has no top-level imports from deleted modules — all broken imports are wrapped in try/except with safe fallbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Create skeleton directories and config.py placeholder</name>
  <files>
    src/db/__init__.py
    src/extractor/__init__.py
    src/config.py
  </files>
  <read_first>
    - src/__init__.py — confirm package root exists
    - src/indexer/ — confirm indexer/ already exists (it survives from v2.0, will be rewritten in Phase 28)
  </read_first>
  <action>
    1. Create src/db/__init__.py with content:
       ```python
       """Database layer — SQLite + FTS5 + backlinks.

       Implemented in Phase 26.
       """
       ```

    2. Create src/extractor/__init__.py with content:
       ```python
       """LLM-based knowledge extraction from git commits.

       Implemented in Phase 28.
       """
       ```

    3. Create src/config.py with content:
       ```python
       """Configuration loading for recall-kg.

       Reads ~/.recall/config.toml. Implemented in Phase 27.
       """
       ```

    4. Verify src/indexer/ already exists (it should from v2.0). Do NOT delete or modify it — Phase 28 will rewrite its contents.

    Per ARCH-03: New layout includes src/db/, src/extractor/, src/indexer/, src/cli/, src/mcp_server/, src/ui_server/, src/config.py.
  </action>
  <verify>
    <automated>cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && python -c "
import os
assert os.path.isfile('src/db/__init__.py'), 'src/db/__init__.py missing'
assert os.path.isfile('src/extractor/__init__.py'), 'src/extractor/__init__.py missing'
assert os.path.isfile('src/config.py'), 'src/config.py missing'
assert os.path.isdir('src/indexer'), 'src/indexer/ missing'
print('All skeleton dirs exist')
"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `test -f src/db/__init__.py && echo exists` prints "exists"
    - [ ] `test -f src/extractor/__init__.py && echo exists` prints "exists"
    - [ ] `test -f src/config.py && echo exists` prints "exists"
    - [ ] `test -d src/indexer && echo exists` prints "exists"
    - [ ] `head -1 src/db/__init__.py` contains "Database layer"
    - [ ] `head -1 src/extractor/__init__.py` contains "LLM-based knowledge extraction"
    - [ ] `head -1 src/config.py` contains "Configuration loading"
  </acceptance_criteria>
  <done>src/db/, src/extractor/ skeleton directories exist with __init__.py. src/config.py placeholder exists. src/indexer/ confirmed present.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `ls src/` shows only: __init__.py, __pycache__, cli/, db/, extractor/, indexer/, mcp_server/, ui_server/, config.py
2. No legacy module names remain
3. ui_server/ has no broken top-level imports from deleted modules
</verification>

<success_criteria>
- All 14 legacy directories deleted (hooks, queue, retention, capture, storage, graph, security, llm, models, gitops, config/)
- tests/ and scripts/ deleted
- graphiti_knowledge_graph.egg-info/ deleted
- src/db/__init__.py, src/extractor/__init__.py, src/config.py exist
- ui_server/ imports from deleted modules are neutralized
</success_criteria>

<output>
After completion, create `.planning/phases/25-teardown/25-01-SUMMARY.md`
</output>
