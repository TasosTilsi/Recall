---
phase: 33-integration-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements:
  - INST-01
must_haves:
  truths:
    - "`pipx install recall-kg` (or `pip install -e .`) succeeds without dependency conflicts"
    - "`recall --help` prints the six-command help text after install"
    - "`rc --help` works as the short alias"
    - "The package name in pyproject.toml is `recall-kg`, not `graphiti-knowledge-graph`"
    - "Entry points `recall` and `rc` both map to `src.cli:cli_entry`"
    - "ui/out/ static assets are bundled into the wheel via package data"
  artifacts:
    - path: "pyproject.toml"
      provides: "recall-kg package config with correct name, entry points, and package data"
      contains: "name = \"recall-kg\""
  key_links:
    - from: "pyproject.toml"
      to: "src/cli.py"
      via: "project.scripts entry points"
      pattern: "recall.*src\\.cli:cli_entry"
    - from: "pyproject.toml"
      to: "ui/out/"
      via: "package-data or MANIFEST inclusion"
      pattern: "package.data|MANIFEST"
---

<objective>
Update pyproject.toml to the final `recall-kg` packaging configuration: correct package name, entry points, dependency list audit, and ui/out/ static asset bundling so `pip install -e .` and wheel builds work cleanly.

Purpose: This is the foundational packaging fix that makes `pipx install recall-kg` work. Without a correct pyproject.toml, the E2E smoke test in Plan 02 cannot run.
Output: Updated `pyproject.toml` with recall-kg name, verified entry points, audited dependencies, and ui/out/ data inclusion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-integration-packaging/33-CONTEXT.md

<interfaces>
<!-- Current pyproject.toml state (as of v2.0 + Phase 25 teardown target) -->

Current name: "graphiti-knowledge-graph" — MUST become "recall-kg"
Current scripts:
  recall = "src.cli:cli_entry"
  rc = "src.cli:cli_entry"

Current packages.find: where = ["."], include = ["src*"]

Known issue (TODO comment in pyproject.toml line 43):
  # TODO: Phase 12 — add MANIFEST.in or data_files for ui/out/ in wheel builds
  # ui/out/ ships via git (pre-built artifact committed); static_dir in ui.py resolves
  # Path(_REPO_ROOT) / "ui/out" which works correctly for both editable and source installs.

The ui/out/ dir must be bundled so that `recall ui` works after pipx install.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package and add ui/out/ data inclusion</name>
  <files>pyproject.toml</files>
  <action>
Read pyproject.toml first to confirm current state, then make these targeted changes:

1. Change package name:
   - `name = "graphiti-knowledge-graph"` → `name = "recall-kg"`

2. Verify entry points are already correct (they should be):
   - `recall = "src.cli:cli_entry"`
   - `rc = "src.cli:cli_entry"`
   If missing, add them under `[project.scripts]`.

3. Add ui/out/ static asset bundling so the built wheel includes the pre-built Next.js output.
   Add a `[tool.setuptools.package-data]` section:
   ```toml
   [tool.setuptools.package-data]
   "*" = ["ui/out/**/*", "ui/out/*"]
   ```
   Also add `MANIFEST.in` file (new file) with:
   ```
   recursive-include ui/out *
   ```
   This ensures sdist and wheel both carry the static assets.

4. Remove the stale TODO comment on lines referencing "Phase 12 — add MANIFEST.in".

5. Audit dependencies: confirm `sentence-transformers` is listed only once (it appears in both `dependencies` and `reranking` optional — move it to `reranking` only if the base install should not require it, or keep in both if it is required for embeddings). Decision: keep `sentence-transformers` in `dependencies` (needed for local embedding via graphiti-core's sentence-transformers embedder).

6. Verify `requires-python = ">=3.12"` is present.

Do NOT change any other dependency versions. Do NOT remove any existing dependencies.
  </action>
  <verify>
    <automated>cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['name']=='recall-kg', f'name={d[\"project\"][\"name\"]}'; scripts=d['project']['scripts']; assert scripts['recall']=='src.cli:cli_entry'; assert scripts['rc']=='src.cli:cli_entry'; print('pyproject.toml OK: name=recall-kg, entry points correct')"</automated>
  </verify>
  <done>pyproject.toml has name="recall-kg", both entry points map to src.cli:cli_entry, ui/out/ data bundling is configured, stale TODO comment removed</done>
</task>

<task type="auto">
  <name>Task 2: Write MANIFEST.in and verify fresh venv install</name>
  <files>MANIFEST.in</files>
  <action>
Create `MANIFEST.in` at repo root (if not already created in Task 1):

```
recursive-include ui/out *
include pyproject.toml
include CLAUDE.md
```

Then verify the packaging works end-to-end with an editable install check:

1. Verify `pip install -e ".[dev]"` works without conflicts by checking that the current `.venv` already satisfies all dependencies:
   ```
   python -c "import graphiti_core, fastapi, typer, mcp, structlog, real_ladybug; print('core deps OK')"
   ```

2. Verify entry point is wired by checking the installed scripts:
   ```
   python -c "import importlib.metadata; ep=importlib.metadata.entry_points(group='console_scripts'); names=[e.name for e in ep]; print('recall' in names or 'entry points:', names)"
   ```

3. If `ui/out/` directory exists, verify at least one file is present (pre-built Next.js):
   ```
   python -c "import os; files=list(os.walk('ui/out')); print(f'ui/out tree: {len(files)} dirs')" 
   ```

Note: If `ui/out/` does not exist yet (UI not yet built by Phase 31), create a placeholder `ui/out/.gitkeep` to satisfy the MANIFEST.in without error. The actual build happens in Phase 31.
  </action>
  <verify>
    <automated>cd /home/tasostilsi/Development/Projects/graphiti-knowledge-graph && python -c "import graphiti_core, fastapi, typer, structlog; print('all core imports OK')" && python -m recall --help 2>&1 | head -5 || .venv/bin/recall --help 2>&1 | head -5 || echo "recall not yet available (upstream phases not complete)"</automated>
  </verify>
  <done>MANIFEST.in exists with ui/out inclusion, core dependencies import cleanly, entry points resolve correctly</done>
</task>

</tasks>

<verification>
- `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['name']=='recall-kg'"` exits 0
- `cat MANIFEST.in` contains `recursive-include ui/out *`
- `python -c "import graphiti_core, fastapi, typer, mcp, structlog"` exits 0
- No duplicate entries in pyproject.toml dependencies
</verification>

<success_criteria>
- pyproject.toml name is "recall-kg"
- Both `recall` and `rc` entry points are defined and point to `src.cli:cli_entry`
- MANIFEST.in includes `ui/out/` recursive include
- [tool.setuptools.package-data] includes ui/out assets
- Stale TODO comment about Phase 12 MANIFEST is removed
- Core Python dependencies import without error
</success_criteria>

<output>
After completion, create `.planning/phases/33-integration-packaging/33-01-SUMMARY.md`
</output>
