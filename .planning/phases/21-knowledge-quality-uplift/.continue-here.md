---
phase: 21-knowledge-quality-uplift
task: post-phase improvements
status: paused
last_updated: 2026-04-03T10:00:20.892Z
---

<current_state>
Phase 21 is COMPLETE and verified. We're paused mid-session after a set of post-phase
improvements that are uncommitted. The remaining ask is a documented config.toml template.
</current_state>

<completed_work>

- Phase 21 fully executed and verified (3/3 plans, VERIFICATION.md passed 5/5)
- fix: test_inject_context_token_budget — _build_option_c signature changed to layer2_items/layer3_items
- fix: structlog stdout pollution — configured to stderr in src/cli/__init__.py (was breaking JSON output tests)
- feat: recall index now shows entity names extracted per commit in --verbose mode
- feat: recall index shows post-run entity name snapshot after completion
- feat: recall note now processes synchronously via service.add() with spinner; falls back to queue on LLM failure
- feat: [indexer] config section — cli (binary) + model fields; env vars RECALL_INDEXER_CLI/MODEL
- feat: capture_mode default changed from "decisions-only" to "decisions-and-patterns"
- feat: indexer_model default set to "haiku" (user confirmed preference)
- feat: ai_cli_available() added alongside claude_cli_available() for configurable binary support
</completed_work>

<remaining_work>

- todo-1: Commit all uncommitted changes (9 modified files + test fix)
- todo-2: Write annotated config.toml template — document every option with valid values and examples
- todo-3: Mark which sections are redundant/optional when Claude Code CLI is installed
- todo-4: Surface template via `recall config --template` or update `recall init` to generate it
</remaining_work>

<decisions_made>

- indexer_model defaults to "haiku" — user confirmed: fast and sufficient for batch extraction
- capture_mode defaults to "decisions-and-patterns" — user wants all captures, not just decisions
- recall note is now synchronous — immediate feedback > silent queue; LLM failure → graceful queue fallback
- structlog → stderr in CLI — fixes JSON stdout corruption; log level WARNING so debug noise suppressed
- [indexer] cli is configurable — enables opencode/other AI CLIs via generic -p interface
- Non-claude CLIs: plain stdout read (no --output-format json); claude gets --model passed explicitly
</decisions_made>

<blockers>
None.
</blockers>

<context>
All tests pass (431 passed, 1 skipped). The transient test_list_compact DB lock failure
is pre-existing and caused by parallel pytest processes holding LadybugDB file lock —
not a regression from any changes this session.

Modified files not yet committed:
  src/cli/__init__.py          — structlog → stderr config
  src/cli/commands/index.py    — entity snapshot output
  src/cli/commands/note_cmd.py — synchronous save with spinner
  src/indexer/extraction.py    — entities in batch result + configurable cli/model
  src/indexer/indexer.py       — _entity_display_name + verbose entity output + sample in return
  src/llm/__init__.py          — make_indexer_llm_client reads config
  src/llm/claude_cli_client.py — _claude_p cli/model params + ai_cli_available()
  src/llm/config.py            — indexer_cli, indexer_model, capture_mode default change
  tests/test_hooks_phase15.py  — layer2_items/layer3_items fix
  ui/src/routes/Dashboard.tsx  — (pre-existing uncommitted change, not from this session)
</context>

<next_action>
Start with: `git add src/ tests/ && git commit -m "feat: CLI logging enhancements + AI CLI configurability"`
Then: write ~/.recall/config.toml template with full inline documentation.
</next_action>
